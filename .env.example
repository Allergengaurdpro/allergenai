# Firebase Configuration
VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_auth_domain_here
VITE_FIREBASE_PROJECT_ID=your_project_id_here
VITE_FIREBASE_STORAGE_BUCKET=your_storage_bucket_here
VITE_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id_here
VITE_FIREBASE_APP_ID=your_app_id_here

# Gemini API Key (PRIMARY - RECOMMENDED for best speed/cost/accuracy)
# Get your key from: https://aistudio.google.com/app/apikey
#
# Setup steps:
# 1. Go to Google AI Studio: https://aistudio.google.com/app/apikey
# 2. Click "Create API Key"
# 3. Select a Google Cloud project (or create new)
# 4. Copy the generated API key
#
# Why Gemini?
# - Context-aware allergen detection (understands ingredients)
# - Structured JSON output (no parsing needed)
# - 75x cheaper than Vision API ($0.02 vs $1.50 per 1,000 images)
# - Faster (single API call vs multi-step pipeline)
# - Detects "may contain" warnings automatically
#
# Pricing: ~$0.02 per 1,000 images
# More info: https://ai.google.dev/pricing
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Google Cloud Vision API Key (FALLBACK - Used if Gemini fails)
# Get your key from: https://console.cloud.google.com/apis/credentials
#
# Setup steps:
# 1. Go to Google Cloud Console: https://console.cloud.google.com/
# 2. Create a new project (or select existing)
# 3. Enable "Cloud Vision API"
# 4. Go to APIs & Services > Credentials
# 5. Create an API key
# 6. (Optional) Restrict the key to "Cloud Vision API" only for security
#
# Pricing: 1,000 images/month FREE, then $1.50 per 1,000 images
# More info: https://cloud.google.com/vision/pricing
VITE_GOOGLE_VISION_API_KEY=your_google_vision_api_key_here
